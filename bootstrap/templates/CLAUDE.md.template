# CLAUDE.md — Claude Code Supplement
# [PROJECT_NAME_PLACEHOLDER]

> Auto-loaded by Claude Code at session start.
> **All governance rules, startup protocol, and session close requirements live in
> `.agent/UNIVERSAL_CONTEXT.md` and `.agent/AGENTS.md` — read those first.**
> This file adds Claude Code-specific behaviour only.

---

## Read First

Before anything else, read:
1. `.agent/UNIVERSAL_CONTEXT.md` — project identity, harness version, key file locations
2. `.agent/AGENTS.md` — mandatory session startup protocol, workflow-first rules,
   absolute prohibitions (the H/S/C/G universal series in §4.1), escalation triggers, and session close requirements

---

## Claude Code-Specific Environment

- **Shell**: Use the `Bash` tool for file ops and git. Use `PowerShell` only when a
  Windows-specific command requires it.
- **Paths in shell**: backslashes. Paths in Python code: forward slashes.
- **Package manager**: [PROJECT_PACKAGE_MANAGER]
- **Test command**: [TEST_RUN_ALL_COMMAND_PLACEHOLDER]
- **Search**: Use the `Grep` tool — bash `grep` is unreliable on Windows.

---

## Optional Stop Hook

Add to `.claude/settings.json` to record session outcome immediately on stop:
```json
{
  "hooks": {
    "Stop": [{"command": "python .agent/scripts/init_session.py --stop-hook"}]
  }
}
```
When present, `outcome_source` is written as `"hook"` (more accurate than `"inferred"`).
When absent, retrospective inference at the next session start is the fallback — no data loss.
