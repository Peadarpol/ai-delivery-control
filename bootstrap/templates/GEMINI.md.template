# GEMINI.md — Gemini / Antigravity IDE Supplement
# [PROJECT_NAME_PLACEHOLDER]

> Auto-loaded by Gemini CLI / Antigravity IDE at session start.
> **All governance rules, startup protocol, and session close requirements live in
> `.agent/UNIVERSAL_CONTEXT.md` and `.agent/AGENTS.md` — read those first.**
> This file adds Gemini/Antigravity-specific behaviour only.

---

## Read First

Before anything else, read:
1. `.agent/UNIVERSAL_CONTEXT.md` — project identity, harness version, key file locations
2. `.agent/AGENTS.md` — mandatory session startup protocol, workflow-first rules,
   absolute prohibitions (P-01 through P-14), escalation triggers, and session close requirements

---

## Gemini / Antigravity-Specific Environment

- **OS**: Windows 11 — use PowerShell syntax for shell commands (`Get-ChildItem`, not `ls`).
- **Package manager**: [PROJECT_PACKAGE_MANAGER]
- **Test command**: [TEST_RUN_ALL_COMMAND_PLACEHOLDER]
- **Layer rules**: When navigating to a specific layer, read the matching
  `.cursor/rules/*.mdc` file if available.
